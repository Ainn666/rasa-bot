build:
  pythonVersion: 3.9

services:
  - type: web
    name: rasa-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: rasa run --enable-api --cors "*" --port 10000
    envVars:
      - key: PORT
        value: 10000
git add render.yaml